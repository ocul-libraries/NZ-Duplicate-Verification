# NZ-Duplicate-Verification
Compares publisher, publication data, and edition statements to verify true duplicates before merging. It uses a CSV input (exported from Alma Analytics) and JSON transformation rules, then outputs a a merge-combine.csv file that can be used with Alma's built in Merge Records and Combine Inventory job.
